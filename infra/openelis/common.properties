# OpenELIS common.properties — required by Spring @PropertySource
# SSL is terminated by nginx; Tomcat runs HTTP-only (see server.xml override).
datasource.url=jdbc:postgresql://openelis-db:5432/clinlims
datasource.username=clinlims
datasource.password=clinlims
