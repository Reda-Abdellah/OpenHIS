FROM nginx:1.25-alpine
# Install the njs (NGINX JavaScript) module from the official nginx repo.
# The package version tracks the installed nginx version automatically.
RUN apk add --no-cache nginx-module-njs
