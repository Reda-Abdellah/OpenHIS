# Archive — superseded planning documents

These are point-in-time plans (transformation, structure, test coverage, uniform auth, feature inventory) whose work has been executed; they are kept for historical context only and are superseded by [`docs/ROADMAP.md`](../../ROADMAP.md), the active documents in [`docs/task_planning/`](../) (`REMEDIATION_PLAN.md`, `4_TODO_list.md`, the defect report), and the current working tree.
